-- TODO COMANDO MYSQL É EM INGLÊS
-- TODO COMANDO TERMINA COM PONTO E VÍRGULA
-- BANCO RELACIONAL SÃO TABELAS QUE SE RELACIONAM ENTRE ELAS
-- TODA TABELA DEVE TER UMA CHAVE PRIMÁRIA
-- CHAVE PRIMÁRIA ELA TORNA A LINHA ÚNICA
-- CAMPO CHAVE PRIMÁRIA NORMALMENTE É UM NÚMERO INTEIRO
-- QUE NÃO SE REPETE

-- CRIAR O BANCO DE DADOS
CREATE DATABASE adsa;

-- UTILIZAR/USAR O BANCO DE DADOS
USE adsa;

-- CRIAR A TABELA ALUNO
CREATE TABLE aluno (
ra CHAR(8) PRIMARY KEY, -- id INT PRIMARY KEY
nome VARCHAR(30),
bairro VARCHAR(20)
);

-- DESCREVER OS CAMPOS DA TABELA
DESCRIBE aluno;
DESC aluno;
SHOW TABLES;

-- INSERIR DADOS NA TABELA
INSERT INTO aluno VALUES
	('01251063', 'Dennis', 'Maria Helena');
    
-- NOSSO PRIMEIRO SELECT
SELECT ra, nome, bairro FROM aluno;
SELECT * FROM aluno;

-- INSERINDO MAIS ALUNOS
INSERT INTO aluno VALUES 
	('01251106', 'Murilo', 'Vila Sônia'),
    ('01251003', 'Thalita com Th', 'Campos Elíseos');
    
SELECT * FROM aluno;

-- EXIBIR O NOME DO ALUNO DENNIS
SELECT nome FROM aluno WHERE nome = 'Dennis';

-- EXIBIR O NOME QUE COMEÇA COM D
SELECT nome FROM aluno WHERE nome LIKE 'D%';

-- EXIBIR O NOME QUE TERMINA COM S
SELECT nome FROM aluno WHERE nome LIKE '%s';

-- EXIBIR O NOME QUE CONTEM A LETRA A
SELECT nome FROM aluno WHERE nome LIKE '%a%';

-- EXIBIR O NOME ONDE A SEGUNDA LETRA É h
SELECT nome FROM aluno WHERE nome LIKE '_h%';

-- EXIBIR O NOME ONDE A PENULTIMA LETRA É t
SELECT nome FROM aluno WHERE nome LIKE '%t_';

-- EXIBIR O NOME ONDE A ANTIPENULTIMA LETRA É t
SELECT nome FROM aluno WHERE nome LIKE '% __';

-- EXIBIR O NOME ONDE A PENULTIMA LETRA É t
SELECT nome FROM aluno WHERE nome LIKE '%t_ %';

-- EXIBIR O NOME QUE É DIFERENTE DE DENNIS
SELECT nome FROM aluno WHERE nome != 'Dennis';
SELECT nome FROM aluno WHERE nome <> 'Dennis';

SELECT * FROM aluno;

-- EXIBIR OS ALUNOS ORDENADOS PELO NOME EM ORDEM CRESCENTE
SELECT * FROM aluno ORDER BY nome ASC;
SELECT * FROM aluno ORDER BY nome;

-- ORDEM DECRESCENTE
SELECT * FROM aluno ORDER BY nome DESC;

-- DESAFIO
-- EXIBIR OS ALUNOS QUE O BAIRRO CONTEM A LETRA S E EXIBIR
	-- EM ORDEM DECRESCENTE DE BAIRRO
SELECT * FROM aluno WHERE bairro LIKE '%s%'
	ORDER BY bairro desc;
    
-- EXCLUIR UMA TABELA
-- DROP TABLE aluno;



-- ADICIONAR COLUNA
ALTER TABLE pessoa ADD COLUMN altura;

-- EXCLUIR COLUNA
ALTER TABLE pessoa DROP COLUMN altura;

-- MODIFICAR A COLUNA NOME PARA VARCHAR(50)
ALTER TABLE pessoa MODIFY COLUMN nome varchar(50)

-- RENOMEAR COLUNA
ALTER TABLE pessoa RENAME dtNasc TO dataNasc